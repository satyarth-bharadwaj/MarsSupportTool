import { render } from "utils/test-utils";
import getClubcardCustomer from "~/routes/dashboard.clubcard"; // Import the component

describe("Clubcard API Testing", () => {
  it("It should have an input bar which takes in Clubcard no.", () => {
    // Render the imported component
    // const { getByLabelText } = render(getClubcardCustomer());

    // Assert that the input bar with the correct label exists
    // expect(getByLabelText("Clubcard Number:")).toBeInTheDocument();
  });
});
